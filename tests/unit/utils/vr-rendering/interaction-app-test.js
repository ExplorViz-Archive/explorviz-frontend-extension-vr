import vrRenderingInteractionApp from 'dummy/utils/vr-rendering/interaction-app';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/interaction app');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingInteractionApp();
  assert.ok(result);
});
