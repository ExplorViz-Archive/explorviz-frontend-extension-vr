import vrRenderingHighlighter from 'dummy/utils/vr-rendering/highlighter';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/highlighter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingHighlighter();
  assert.ok(result);
});
