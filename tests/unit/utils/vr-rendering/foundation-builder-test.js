import vrRenderingFoundationBuilder from 'dummy/utils/vr-rendering/foundation-builder';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/foundation builder');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingFoundationBuilder();
  assert.ok(result);
});
