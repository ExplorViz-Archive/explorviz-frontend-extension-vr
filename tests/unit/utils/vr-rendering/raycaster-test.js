import vrRenderingRaycaster from 'dummy/utils/vr-rendering/raycaster';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/raycaster');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingRaycaster();
  assert.ok(result);
});
