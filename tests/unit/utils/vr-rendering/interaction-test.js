import vrRenderingInteraction from 'dummy/utils/vr-rendering/interaction';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/interaction');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingInteraction();
  assert.ok(result);
});
