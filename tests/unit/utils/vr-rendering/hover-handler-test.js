import vrRenderingHoverHandler from 'dummy/utils/vr-rendering/hover-handler';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/hover handler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingHoverHandler();
  assert.ok(result);
});
