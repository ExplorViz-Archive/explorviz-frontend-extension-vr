import vrRenderingCenterAndZoomCalculator from 'dummy/utils/vr-rendering/center-and-zoom-calculator';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/center and zoom calculator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingCenterAndZoomCalculator();
  assert.ok(result);
});
