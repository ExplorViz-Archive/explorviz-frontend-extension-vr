import vrRenderingKlayLayouter from 'dummy/utils/vr-rendering/klay-layouter';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/klay layouter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingKlayLayouter();
  assert.ok(result);
});
