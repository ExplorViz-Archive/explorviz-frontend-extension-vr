import vrRenderingLabeler from 'dummy/utils/vr-rendering/labeler';
import { module, test } from 'qunit';

module('Unit | Utility | vr rendering/labeler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vrRenderingLabeler();
  assert.ok(result);
});
