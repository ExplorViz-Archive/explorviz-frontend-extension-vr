import raycaster from 'dummy/utils/raycaster';
import { module, test } from 'qunit';

module('Unit | Utility | raycaster');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = raycaster();
  assert.ok(result);
});
